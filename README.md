# VoltaVPN

VoltaVPN — это будущий простой VPN‑клиент для Windows с упором на безопасность и открытый код.
Сейчас проект находится на **ранней стадии разработки**: реализован только технический каркас, без какой‑либо VPN‑логики.

---

## Что мы хотим получить в итоге

- **Удобный клиент для пользователей** Windows
- **Минимум лишних настроек** — фокус на «подключиться в один клик»
- **Прозрачная безопасность**:
  - открытый исходный код;
  - понятные политики хранения и обработки ключей;
  - отсутствие трекинга пользователей.

Конкретные фичи (типы VPN, протоколы, формат ключей и т.п.) сейчас обсуждаются и будут задокументированы отдельно, когда реализация станет публичной.

---

## Что уже сделано

- Базовый каркас проекта на Go:
  - структура каталогов (`cmd/`, `internal/`, `pkg/`);
  - модуль `go.mod`, подключающий GUI‑фреймворк.
- Минимальное десктопное окно на базе **Fyne** (без сетевого кода и VPN‑функций).
- Инфраструктура для разработки:
  - `.gitignore`, `.editorconfig`, `Makefile` с целями `fmt`, `vet`, `build`;
  - `go.sum` с зафиксированными зависимостями.
- Первичные документы по безопасности:
  - `SECURITY.md` — как сообщать об уязвимостях и базовые принципы;
  - `threat-model.md` — черновая модель угроз для клиента;
  - `POLICIES.md` — политики разработки (запрет логирования секретов, запрет хранения ключей в репо, ограничения на exec/shell).

Вся текущая реализация — это **скелет** будущего клиента. Подключение к VPN, работа с ключами и протоколами появятся позднее и будут описаны отдельно.

---

## Статус проекта

🚧 Проект в активной разработке. Клиент пока **не готов к использованию конечными пользователями**.

Если вы разработчик и хотите помочь — взгляните на код и документы в репозитории, особенно на `POLICIES.md` и `SECURITY.md`.

---

## Лицензия

Лицензия планируется как открытая (например, Apache‑2.0), но окончательный текст ещё не зафиксирован и может измениться до первого стабильного релиза.